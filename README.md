# bundle-adjustment-based-on-3D-2D-matched-points
using bundle adjustment to optimize the camera pose and 3D points position based on 3D-2D matched points 
```cmake
mkdir build
cd build
cmake ..
make
./my_BA
```